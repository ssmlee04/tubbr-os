# Database - replace with actual values in production
DATABASE_URL="postgres://tubbr:tubbr@localhost:5432/tubbr"  # pragma: allowlist secret

# Phoenix - replace with generated values in production
SECRET_KEY_BASE="replace-this-in-production"  # pragma: allowlist secret
PORT=4000
PHX_HOST=localhost

# JWT - replace with secure secret in production
JWT_SECRET="replace-this-in-production"  # pragma: allowlist secret
